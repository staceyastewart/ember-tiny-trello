<div class="col-md-3">
  <div class="panel panel-default text-center">
    <span class="border-top-0">
      {{#each model as |list|}}
        {{user-list title=list.title}}
      {{/each}}
    </span>
    <span class="border-top-0">
      {{#link-to "board"}}New List{{/link-to}}
    </span>
  </div>
</div>
