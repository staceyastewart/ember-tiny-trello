{{#if isAdding}}
  <!--make below line reusable  -->
  {{#if form}}
    <!-- <form {{!action "sendDataToController" listId item on="submit"}}>
      {{!input value=item placeholder="Add New Item to List"}}
    </form> -->
    {{form-component placeholder="THIS ITEM PLACEHOLDER" newItem=(action newItem)}}
  {{else}}
    {{!input value=text enter="sendDataToController" placeholder="Name Your New List"}}
    {{form-component placeholder="Add A New List" newList=(action newList)}}

    <!-- <form {{!action "sendDataToController" actionable="actionable" on="submit"}}>
      {{!input value=text placeholder="Name new list" action="newList"}}
    </form> -->

  {{/if}}
{{else}}
  <span onClick={{action "toggleIsAdding"}}>
    {{#link-to link}}
      {{linkText}}
    {{/link-to}}
  </span>
{{/if}}
