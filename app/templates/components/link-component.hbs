{{#if isAdding}}
  {{#if item}}
    {{form-component placeholder="THIS ITEM PLACEHOLDER" newItem=(action newItem) listId=listId toggleIsAdding=(action "toggleIsAdding")}}
  {{else}}
    {{form-component placeholder="Add A New List" newList=(action newList) toggleIsAdding=(action "toggleIsAdding")}}
  {{/if}}
{{else}}
  <span onClick={{action "toggleIsAdding"}}>
    {{#link-to link}}
      {{linkText}}
    {{/link-to}}
  </span>
{{/if}}
