<h2>{{title}}</h2>

{{list-item items=items}}

<!-- <form {{!action "sendDataToController" listId item on="submit"}}>
  {{!input value=item placeholder="Add New Item to List"}}
</form> -->

{{!form-component placeholder="THIS ITEM PLACEHOLDER" newItem=(action newItem) listId=listId}}

{{link-component addInput="addInput" linkText="Add A THING" newItem=(action newItem) item="true" listId=listId}}
